{
  "name": "chatroom-app",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-google": "^0.7.0",
    "eslint-plugin-html": "^2.0.0"
  },
  "scripts": {
    "postinstall": "./node_modules/.bin/bower install",
    "lint": "npm run lint:javascript && polymer lint",
    "lint:javascript": "eslint . --ext js,html --ignore-path .gitignore",
    "test": "./node_modules/.bin/polymer test",
    "start": "./node_modules/.bin/polymer serve --open",
    "start:dist": "./node_modules/.bin/polymer serve build/bundled --open",
    "build": "./node_modules/.bin/polymer build --verbose"
  },
  "dependencies": {
    "bower": "^1.8.2",
    "polymer-cli": "^1.5.7"
  }
}
